# سجل التغييرات (Changelog)

يوثّق هذا الملف كل التغييرات الجوهرية على مشروع AquaBill JO، بترتيب زمني عكسي (الأحدث أولاً).
يتبع المشروع مبدأ [الترقيم الدلالي](https://semver.org/lang/ar/) (Semantic Versioning): `major.minor.patch`.

---
## [1.0.0] — 2026-08-12

### 🎨 الأيقونات الهوية البصرية
- استبدال شعار العلامة التجارية بالكامل (قطرة + آلة حاسبة) في الترويسة والتذييل وكافة الأيقونات.
- إضافة نظام أيقونات احترافي شامل: `favicon.ico` (متعدد المقاسات)، `favicon.svg`، `favicon-16.png`، `favicon-32.png`، `apple-touch-icon.png` (180×180)، `android-chrome-192.png`، `android-chrome-512.png`، و `og-image.png` (1200×630).
- اعتماد ملف `manifest.webmanifest` المحدث بأيقونات جديدة ودعم خاصية `maskable`.

### ⚙️ الأداء والصيانة
- ترقية رقم إصدار التطبيق رسمياً إلى `1.0.0`.
- ترقية إصدار كاش الـ Service Worker إلى `v12.0` وتحديث قائمة الملفات المخزَّنة بالكامل للعمل الأوفلاين.
---
## [1.0.0] — 2026-08-12

### أُضيف
- استبدال شعار العلامة التجارية بالكامل (قطرة + آلة حاسبة) بالترويسة والتذييل وكل الأيقونات.
- نظام أيقونات احترافي كامل: `favicon.ico` (متعدد المقاسات)، `favicon.svg`، `favicon-16.png`، `favicon-32.png`، `apple-touch-icon.png` (180×180)، `android-chrome-192.png`، `android-chrome-512.png`، `og-image.png` (1200×630).
- ملف `manifest.webmanifest` الجديد (يحل محل `manifest.json`) بأيقونات محدَّثة ودعم `maskable`.

### تغيّر
- ترقية إصدار كاش الـ Service Worker إلى `v12.0` وتحديث قائمة الملفات المخزَّنة بالكامل.
- ترقية رقم إصدار التطبيق إلى `1.0.0
---

## [1.0.0] — 2026-08-12

### أُضيف
- الاسم التجاري **AquaBill JO** وشعار احترافي جديد (قطرة + حلقة ذهبية + علامة صح).
- زر تبديل يدوي بين الوضع الفاتح والداكن، مع حفظ الاختيار محلياً.
- ملف `config.js` مركزي يجمع كل ثوابت التطبيق (الاسم، الإصدار، التعرفة، مفاتيح التخزين).
- تسجيل أخطاء العميل محلياً (بدون أي خادم خارجي).
- تصدير واستيراد إعدادات المستخدم كملف JSON.
- بيانات منظّمة (JSON-LD) ووسم `robots` لتحسين ظهور الأداة بمحركات البحث.
- رابط "تخطَّ إلى المحتوى" وتحسينات وصولية إضافية (`aria-live`, `aria-pressed`, `focus-visible`).
- حركات وانتقالات خفيفة، مع احترام تفضيل "تقليل الحركة" بالنظام.
- ملفات توثيق جديدة: `CHANGELOG.md`, `ROADMAP.md`, `LICENSE`, `CONTRIBUTING.md`, `robots.txt`, `sitemap.xml`.

### أُصلح
- تباين ضعيف لألوان العناوين والقيم بالوضع الداكن (كانت تستخدم لون العلامة التجارية الداكن نفسه فوق خلفية داكنة).
- أنماط CSS كانت مضمّنة (inline) بدل ملف الأنماط المركزي — نُقلت بالكامل.

### تغيّر
- صياغة كل النصوص الموجّهة للمستخدم من الجمع إلى المفرد.
- ترقية إصدار كاش الـ Service Worker إلى `v12.0`.

---

## [1.0.0] — 2026-08-12

### أُضيف
- الإصدار الأول الكامل: حاسبة الفاتورة، جدول التعرفة القابل للتعديل بقفل رمزي، مقارنة العداد بصهريج المياه، دعم PWA كامل (Manifest + Service Worker)، وسوم Open Graph وTwitter Card.
