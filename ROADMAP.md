# خارطة الطريق (Roadmap)

أفكار وتحسينات مستقبلية محتملة لمشروع AquaBill JO. هذه القائمة **غير ملزمة** وتعتمد على الوقت والحاجة الفعلية — وليست وعداً بميزات قادمة بموعد محدد.

## قيد الدراسة

- **مخطط بياني لتطوّر الاستهلاك الشهري** عبر الأشهر (سيحتاج قسم "Charts" بـ `style.css`، المحجوز حالياً وغير مُفعَّل).
- **دعم شركات مياه إضافية** بقائمة منسدلة جاهزة (بدل تعديل الأرقام يدوياً) إذا اختلفت التعرفة مستقبلاً بين المحافظات.
- **تصدير الفاتورة كملف PDF** بدل JSON فقط، للأرشفة أو الطباعة.
- **حماية حقيقية على مستوى الخادم** لقفل تعديل التعرفة (عبر Cloudflare Access أو ما شابه) لمن يريد نشرها للعامة بثقة أكبر.
- **واجهة إنجليزية اختيارية** (i18n) لغير الناطقين بالعربية المقيمين بالأردن.
- **ويدجت "المتابعة الأسبوعية"** لتسجيل قراءة العداد أسبوعياً وحساب الإجمالي التراكمي تلقائياً داخل الأداة نفسها.

## غير مخطط له حالياً

- أي اتصال بخادم خارجي أو قاعدة بيانات — يتعارض مع مبدأ الأداة الأساسي: العمل بالكامل من المتصفح بدون أي خادم.
- تطبيق متجر Google Play/App Store — النسخة الحالية (PWA) تُثبَّت مباشرة من المتصفح بنفس تجربة تطبيق حقيقي، بدون الحاجة لحساب مطور أو مراجعة متجر.
